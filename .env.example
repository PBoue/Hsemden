# Copy this file to .env and adjust values.
#
# IMPORTANT: Vite embeds VITE_* variables into the client bundle.
# This is a "nice-looking gate", not real security.

# Require login gate (set to false for static prerendering / public builds)
VITE_REQUIRE_AUTH=true

# Static credentials
VITE_AUTH_USERNAME=interview
VITE_AUTH_PASSWORD=interview
